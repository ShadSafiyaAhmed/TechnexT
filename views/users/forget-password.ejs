<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="../assets2/css/vendors/signup.css" rel="stylesheet">
    <style>
        /* Add styles to the body or a container element to set a background or overall styles */
        body {
            background-color: #f0f0f0;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Style the forget-logo */
        .forget-logo {
            max-height: 100px;
            margin: 0 auto;
        }

        /* Style the title-text */
        .title-text {
            text-align: center;
            margin-top: 20px;
        }

        /* Style the title */
        .title.reset-password {
            font-size: 24px;
            font-weight: bold;
            color: #333;
        }

        /* Style the form */
        .form-inner {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            margin: 0 auto;
        }

        /* Style the form fields */
        .field {
            margin-bottom: 20px;
            margin-right: 20px;
        }

        .field input[type="password"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 3px;
        }

        /* Style the submit button */
        .field.btn {
            text-align: center;
        }

        .field.btn input[type="submit"] {
            background-color: #007bff;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 3px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .field.btn input[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
    <title>Reset Password</title>
</head>
<body>
    <div class="wrapper">
        <div class="centered-form">
            <img class="forget-logo" src="/images/download.png" alt="">
            <div class="title-text">
                <div class="title reset-password">Reset password</div>
            </div>
            <div class="form-inner">
                <form action="/forget-password" method="post" class="login">
                    <div class="field">
                        <input type="password" name="password" placeholder="Password" required>
                    </div>
                    <div class="field">
                        <input type="hidden" name="user_id" value="<%=user_id%>">
                    </div>
                    <div class="field btn">
                        <div class="btn-layer"></div>
                        <input type="submit" value="Reset password">
                    </div>
                    <%= typeof message !== 'undefined' ? message : '' %>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
